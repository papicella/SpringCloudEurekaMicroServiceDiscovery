spring:
  application:
    name: ribbon-client-apples
  cloud:
    services:
      registrationMethod: route

producer:
  ribbon:
    ServerListRefreshInterval: 5000
    IsSecure: false

security:
  basic:
    enabled: false
